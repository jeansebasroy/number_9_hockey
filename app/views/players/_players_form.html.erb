<%= render 'shared/error_messages', object: player.object %>
      		
<%= player.label :first_name, "First Name:" %>
<%= player.text_field :first_name %>

<%= player.label :last_name, "First Name:" %>
<%= player.text_field :last_name %>

<%= player.label :date_of_birth, "Date of Birth:" %>
<%= player.date_select :date_of_birth %>

<%= player.label :shoots, 'Shoots:' %>
<%= player.select :shoots, options_for_select([['Select One', ''], ['Left','Left'], ['Right', 'Right']]) %>

<%#= camp.label :skill, 'Select Skills (multiple):' %>
<%#= camp.select :publish_date, options_from_collection_for_select(Skill.all, :id, :name), :multiple => true %>